<nav class="navbar navbar-expand-lg navbar-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="{{path('admin_index')}}">Admin</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link {{app.request.pathinfo == '/admin/' ? 'active' : false}}" href="{{path('admin_index')}}">Projets</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {{app.request.pathinfo == '/admin/technologies' ? 'active' : false}}" href="{{path('admin_technologies')}}">Technologies</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {{app.request.pathinfo == '/admin/contributors' ? 'active' : false}}" href="{{path('admin_contributors')}}">Collaborateurs</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {{app.request.pathinfo == '/admin/images' ? 'active' : false}}" href="{{path('admin_images')}}">Images</a>
          </li>
        </ul>
        <ul class="navbar-nav d-flex align-items-end mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" href="{{path('app_logout')}}">Déconnexion</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>